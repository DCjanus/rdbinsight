//! TODO: 实现一个覆盖面广的回归测试：启动多个不同版本的 Redis 实例，
//! 使用共享数据集写入尽可能多的数据类型，导出 RDB 文件并将解析结果与
//! 预期数据集比对，以取代此前 `tests/` 目录中几乎为每种数据类型单独维护
//! 用例的方式，从而降低维护成本；同时根据各版本的特性限制数据项的出现，
//! 避免在不支持的版本上校验。除此之外，保留若干针对特定场景的轻量级测试，
//! 弥补大用例难以覆盖的角落。为每个数据项抽象一个结构体，记录最低支持的
//! Redis 版本、写入命令以及预期解析结果，以便几乎正交地维护 Redis 版本列表
//! 与测试命令列表，并将它们组装成一个大的 `TestCase`，同时兼顾版本差异化能力。
//! TODO: 完成实现后请删除此段中文注释，避免在代码合入后仍保留过期说明。
